# jwt-peek

Small CLI to decode a JWT without pasting it into some random website.

```bash
dotnet run -- eyJhbGciOiJIUzI1NiIs...
```

Prints the header and payload as formatted JSON. That's it for now.
